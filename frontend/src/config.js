// Unified configuration for backend API URL
// Change this value to point frontend to any backend instance
const API_BASE_URL =  "https://student-mangemnt-test.onrender.com";

const config = {
  API_BASE_URL,
};

export default config;
